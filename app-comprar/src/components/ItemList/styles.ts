import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    emptyContainer: { 
        flex: 1, 
        justifyContent: 'flex-start', 
        marginTop: 20, 
        alignItems: 'center' },
    emptyText: {
        color: "#828282"
    }
})